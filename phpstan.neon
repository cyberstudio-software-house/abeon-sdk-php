includes:
    # Pre-existing level-8 findings captured 2026-05-27 so the gate blocks NEW
    # violations. These 7 are tracked tech-debt (AppsController/PreferencesController
    # /JwtValidator/AbeonConfig/RabbitMq/ApiResponse) to burn down later.
    - phpstan-baseline.neon

parameters:
    level: 8
    paths:
        - src
